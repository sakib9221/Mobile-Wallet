package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Premium Brand Palette (Teal & Orange)
val TealPrimary = Color(0xFF0D9488)      // Core brand Teal
val TealDark = Color(0xFF0F766E)
val LightTeal = Color(0xFF2DD4BF)       // Light Teal accent for dark mode

val OrangeSecondary = Color(0xFFF97316)  // Core brand Orange
val LightOrange = Color(0xFFFB923C)      // Light Orange accent for dark mode

// Light Mode backgrounds and surfaces
val BackgroundLight = Color(0xFFF8FAFC)  // Soft slate gray
val SurfaceLight = Color.White

// Dark Mode backgrounds and surfaces
val BackgroundDark = Color(0xFF0F172A)   // Rich midnight blue-gray
val SurfaceDark = Color(0xFF1E293B)      // Elevate dark surface
val SurfaceVariantDark = Color(0xFF334155) // Card borders and secondary surfaces
